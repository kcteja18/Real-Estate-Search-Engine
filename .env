

# .env.example
# Copy this file to .env and fill in your values

# --- Database Configuration ---
# Your schema supports PostgreSQL or SQLite.
# For PostgreSQL:
# DATABASE_URL="postgresql+psycopg2://realestate:password@localhost:5432/realestate_db"
# For SQLite (default in your scripts):
DATABASE_URL="sqlite:///./realestate.db"

# --- File Paths ---
# These paths are used by ETL_pipeline.py.
# The FastAPI server will use the 'PROPERTY_DATA_FILE' for the /ingest default
# and 'MODEL_CHECKPOINT_PATH' for loading the model.

# Path to the default Excel file (used if ETL is run directly)
PROPERTY_DATA_FILE="D:/IITGN/Placement_prep/assets/assets/Property_list.xlsx"

# Path to the main assets directory (images, certificates)
ASSETS_DIR="D:/IITGN/Placement_prep/assets/assets"

# Path to the AI model checkpoint
MODEL_CHECKPOINT_PATH="D:/IITGN/Placement_prep/Real-Estate-Search-Engine/src/training/checkpoint_epoch_50.pth"
# Path to store the generated FAISS index and metadata
VECTOR_DB_PATH="D:/IITGN/Placement_prep/Real-Estate-Search-Engine/src/data ingestion/vector_db"


# .env.example
# Paths for Docker container (use relative paths)


# DATABASE_URL="sqlite:///./realestate.db"

# # --- File Paths ---
# # These paths are relative to the app's root directory
# PROPERTY_DATA_FILE="./assets/Property_list.xlsx"
# ASSETS_DIR="./assets"
# MODEL_CHECKPOINT_PATH="checkpoint_epoch_50.pth"
# VECTOR_DB_PATH="./vector_db"